import { useState } from "react";
import { motion } from "framer-motion";
import clsx from "clsx";
import Head from "next/head";
import Image from "next/image";
import styles from "@/styles/Home.module.css";
import { SearchInput } from "@/components/SearchInput";
import { Suggestions } from "@/components/Suggestions";

export default function Home() {
  const [showingResult, setShowingResult] = useState(false);

  const handleSearch = () => setShowingResult(true);

  return (
    <>
      <Head>
        <title>SmartChat</title>
        <meta name="description" content="Generated by create next app" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <main className={clsx(styles.main, showingResult && styles.withResult)}>
        <div className={styles.description}>
          <p>
            Visualize your data with&nbsp;
            <code className={styles.code}>SmartChat</code>
            &nbsp;by asking anything
          </p>
          <div className={styles.builtWith}>
            <span>Built with </span>
            <a
              href="https://en.pingcap.com/tidb-cloud/?utm_source=smartchat&utm_medium=referral"
              target="_blank"
              rel="noopener noreferrer"
            >
              <Image
                src="/tidb.svg"
                alt="TiDB Cloud Logo"
                className={styles.vercelLogo}
                width={138}
                height={24}
                priority
              />
            </a>
            <span>x</span>
            <a
              href="https://vercel.com?utm_source=create-next-app&utm_medium=default-template&utm_campaign=create-next-app"
              target="_blank"
              rel="noopener noreferrer"
            >
              <Image
                src="/vercel.svg"
                alt="Vercel Logo"
                className={styles.vercelLogo}
                width={100}
                height={24}
                priority
              />
            </a>
          </div>
        </div>

        <motion.div layout className={styles.content}>
          <SearchInput onSearch={handleSearch} showingResult={showingResult} />
        </motion.div>

        {!showingResult && <Suggestions showResult={showingResult} />}
      </main>
    </>
  );
}
